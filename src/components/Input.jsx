import styled from "styled-components";

const Input = styled.input`
    border-radius: 5px;
    padding: 5px;
    font-size: 16px;
    font-weight: bold;

    :focus {
        outline: none;
    }

`;

export default Input;